{
  "name": "scroll-break",
  "version": "1.0.0",
  "description": "Take a break from YouTube Shorts",
  "type": "module",
  "scripts": {
    "build": "vite build --config vite.content.config.ts && vite build && vite build --config vite.background.config.ts",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "typescript": "^5.3.3",
    "vite": "^5.0.10"
  }
}
